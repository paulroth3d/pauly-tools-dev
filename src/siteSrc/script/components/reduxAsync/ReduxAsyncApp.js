import React from 'react';
import PropTypes from 'prop-types';

const ReduxAsyncApp = (props) => {
  return (
    <span>This is the redux async app</span>
  );
}

ReduxAsyncApp.propTypes = {
};

export default ReduxAsyncApp;
